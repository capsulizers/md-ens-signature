/** The public Sepolia endpoint the page reads ENS records from by default. */
export const DEFAULT_RPC_URL = "https://ethereum-sepolia-rpc.publicnode.com";

/** The parent ENS name whose members may sign, as set up for the demo. */
export const DEFAULT_PARENT_NAME = "alice.eth";
